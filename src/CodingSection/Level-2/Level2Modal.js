import Modal from "../../UI/Modal";

export const Level2Modal = ({ hideModal }) => {
  return (
    <Modal hideModal={hideModal}>
      <h2>Let's get started with level 2!</h2>
      <p>hello</p>
    </Modal>
  );
};
