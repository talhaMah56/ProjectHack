import Modal from "../../UI/Modal";

export const Level1Modal = ({ hideModal }) => {
  return (
    <Modal hideModal={hideModal}>
      <h2>Let's get started with level 1!</h2>
      <p>hello</p>
    </Modal>
  );
};
